.row
  h1.title
    = t('page.listing', model: t('activerecord.models.user_reserve_stat'))
  #content_list
    table.table.table-striped.index
      tr
        th
          = t('activerecord.attributes.user_reserve_stat.start_date')
        th
          = t('activerecord.attributes.user_reserve_stat.end_date')
        th
          = t('activerecord.attributes.user_reserve_stat.state')
        th
      - @user_reserve_stats.each do |user_reserve_stat|
        |  <tr class="line
        = cycle("0", "1")
        | "> 
        td
          = l(user_reserve_stat.start_date)
        td
          = l(user_reserve_stat.end_date)
        td
          = localized_state(user_reserve_stat.current_state)
        td
          = link_to t('page.show'), user_reserve_stat
          = link_to t('page.destroy'), user_reserve_stat, data: {confirm: t('page.are_you_sure')}, method: :delete
    = paginate(@user_reserve_stats)
#submenu.col-md-3
  ul
    li
      = link_to t('page.new', model: t('activerecord.models.user_reserve_stat')), new_user_reserve_stat_path
