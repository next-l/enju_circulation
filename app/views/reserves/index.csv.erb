title,isbn,item_identifier,call_number,username,created_at
<%- @reserves.each do |reserve| -%>
"<%=h reserve.manifestation.original_title.gsub(/"/, '""') -%>",<%=h reserve.manifestation.isbn %>,<%=h reserve.item.try(:item_identifier) -%>,<%=h reserve.item.try(:call_number) -%>,<%= reserve.user.try(:username) %>,<%=h reserve.item.try(:created_at) %><%= "\n" -%>
<%- end -%>
